# Backend API for the modern web frontend. Reuses the existing pipeline/app.py.
# Install on top of the project's existing environment (which already has gradio,
# the pipeline deps, etc.):  pip install -r webapp/backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
