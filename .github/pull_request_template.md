<!-- Thanks for contributing! Keep PRs small and focused where you can. -->

## What & why

<!-- What does this change do, and why? Link any related issue (e.g. Closes #123). -->

## How tested

<!-- e.g. `pytest tests/`, `make lint`, frontend build, manual steps. -->

## Checklist

- [ ] `.venv/bin/python -m pytest tests/` passes
- [ ] `make lint` passes (ruff)
- [ ] Frontend builds (`cd webapp/frontend && npm run build`) — if frontend touched
- [ ] No secrets/credentials committed
- [ ] Didn't make a non-commercial model the default (see `THIRD_PARTY_NOTICES.md`)
- [ ] Docs updated if behavior/setup changed
