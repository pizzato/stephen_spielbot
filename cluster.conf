# Remote worker hostnames — one per line.
# Each host runs ComfyUI (video generation) and F5-TTS (narration).
# The local machine is always included automatically.
# Comment out or remove lines to exclude workers.
s1
s2
s3

